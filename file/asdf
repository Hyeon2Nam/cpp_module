다음 공용 static 멤버 함수 오버로드를 클래스에 추가합니다.

두 고정점 값을 참조하는 static 멤버 함수 'min'과
가장 작은 값에 대한 참조와 참조를 가져오는 오버로드를 반환합니다.
두 개의 상수 고정점 값을 지정하고
가장 작은 상수 값에 대한 참조를 반환합니다.

• 두 고정점 값을 참조하는 static 멤버 함수 'max'
그리고 가장 큰 값에 대한 참조와 참조를 사용하는 오버로드를 반환합니다.
두 개의 상수 고정점 값에서 가장 큰 상수 값에 대한 참조를 반환합니다.
